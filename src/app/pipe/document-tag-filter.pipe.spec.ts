import { DocumentTagFilterPipe } from './document-tag-filter.pipe';

describe('DocumentTagFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTagFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
