import { DocumentNameFilterPipe } from './document-name-filter.pipe';

describe('DocumentNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
